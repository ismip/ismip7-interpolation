# Variables with no spatial (x,y) grid -- dim = t only in the ISMIP7
# variable spec. These are domain-integrated scalar time series
# (e.g. total ice mass) and cannot be regridded (cdo setgrid/remap
# require an x,y grid). They are placed in the output directory
# unchanged, regardless of target resolution -- symlinked by default,
# or copied/skipped via --on-unchanged (see place_unchanged() in
# lib/common.sh).
#
# Source: ISMIP7_variable_request.csv
# (https://raw.githubusercontent.com/ismip/ISM_SimulationChecker/refs/heads/main/isschecker/data/ISMIP7_variable_request.csv)
lim
limnsw
iareagr
iareafl
tendacabf
tendlibmassbfgr
tendlibmassbffl
tendlicalvf
tendlifmassbf
tendligroundf
